/** @type {import('next/config').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      unoptimized: true,
    },
    basePath: '/varma.github.io', // Replace with your repository name
  }
  
  module.exports = nextConfig